import { SerializedSignature } from '../cryptography/signature';
/** Verify data that is signed with `signer.signMessage`. */
export declare function verifyMessage(message: Uint8Array | string, serializedSignature: SerializedSignature): Promise<boolean>;
